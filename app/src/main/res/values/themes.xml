<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Scooter" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Scooter" parent="Base.Theme.Scooter" />

    <style name="customOutlinedBox" parent="Widget.Material3.TextInputLayout.OutlinedBox">
        <item name="boxStrokeColor">@drawable/outlined_color_selector</item>
        <item name="boxStrokeWidth">2dp</item>
        <item name="boxCornerRadiusTopStart">10dp</item>
        <item name="boxCornerRadiusTopEnd">10dp</item>
        <item name="boxCornerRadiusBottomStart">10dp</item>
        <item name="boxCornerRadiusBottomEnd">10dp</item>
        <item name="colorPrimary">@color/textGray</item>
        <item name="android:textColorHint">@color/textGray</item>
    </style>

    <style name="customOutlinedButton" parent="Widget.Material3.Button">
        <item name="backgroundTint">@color/blue</item>
        <item name="cornerRadius">12dp</item>
        <item name="android:layout_margin">15dp</item>
        <item name="strokeWidth">2dp</item>
        <item name="android:textSize">20sp</item>
    </style>
</resources>